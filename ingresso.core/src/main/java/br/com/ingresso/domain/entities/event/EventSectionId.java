package br.com.ingresso.domain.entities.event;

import br.com.ingresso.common.domain.value.object.Id;

import java.util.UUID;

public class EventSectionId extends Id {

    public EventSectionId(UUID id) {
        super(id);
    }
}
