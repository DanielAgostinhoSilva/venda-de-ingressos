package br.com.ingresso.domain.entities.partner;

import br.com.ingresso.common.domain.value.object.Id;

import java.util.UUID;

public class PartnerId extends Id {

    public PartnerId(UUID id) {
        super(id);
    }
}
